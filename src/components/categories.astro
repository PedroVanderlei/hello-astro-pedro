---
import type { CollectionEntry } from 'astro:content'
import { getEntries } from 'astro:content'

const { categories } = Astro.props

const cats = await getEntries(categories)
---

{
  categories && (
    <div class="flex flex-row flex-wrap items-start gap-2 pb-2">
      {cats.map((category: CollectionEntry<'category'>) => (
        <a
          itemprop="articleSection"
          href={`${import.meta.env.BASE_URL}category/${encodeURIComponent(category.id)}/`}
          class="inline-block rounded border border-amber-200/80 bg-amber-800 px-3 py-1 text-xs font-medium tracking-tight text-white uppercase hover:bg-amber-700 dark:border-amber-900/40 dark:bg-amber-200 dark:text-gray-950 dark:hover:bg-amber-100"
        >
          {category.data.title}
          <span class="sr-only">category</span>
        </a>
      ))}
    </div>
  )
}
